export CLUSTER_NAME=$(aws eks describe-cluster --region us-east-1 --name hpa-demo --query "cluster.name" --output text)

export CLUSTER_REGION=$(aws eks describe-cluster --name ${CLUSTER_NAME} --region us-east-1 --query "cluster.arn" --output text | cut -d: -f4)

eksctl create iamserviceaccount --name eksdemo-secretmanager-sa --region=us-east-1 --cluster hpa-demo --attach-policy-arn "$POLICY_ARN" --approve --override-existing-serviceaccounts


eksctl create iamserviceaccount --name eksdemo-secretmanager-sa --region=us-east-1 --cluster hpa-demo --attach-policy-arn "$POLICY_ARN" --approve --override-existing-serviceaccounts


eksctl delete iamserviceaccount --cluster="$CLUSTER_NAME" --name=eksdemo-secretmanager-sa --region="$CLUSTER_REGION"